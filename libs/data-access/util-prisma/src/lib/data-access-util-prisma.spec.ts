import { dataAccessUtilPrisma } from './data-access-util-prisma';

describe('dataAccessUtilPrisma', () => {
  it('should work', () => {
    expect(dataAccessUtilPrisma()).toEqual('data-access-util-prisma');
  });
});
