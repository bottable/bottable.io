import { apiWrapperUtilSlack } from './api-wrapper-util-slack';

describe('apiWrapperUtilSlack', () => {
  it('should work', () => {
    expect(apiWrapperUtilSlack()).toEqual('api-wrapper-util-slack');
  });
});
