import { apiWrapperUtilMail } from './api-wrapper-util-mail';

describe('apiWrapperUtilMail', () => {
  it('should work', () => {
    expect(apiWrapperUtilMail()).toEqual('api-wrapper-util-mail');
  });
});
